const li = document.createElement('li');
const test_text  = "You need to solve the murder by finding the right suspect, the right murder weapon, and the correct airport where the murder happened.\n"
const printing_text = document.getElementById('printing_text')

printing_text.appendChild(li).textContent = test_text
